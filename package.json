{
  "name": "csc-307-app-redo",
  "version": "1.0.0",
  "description": "Yay, a React app!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd packages/react-frontend && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "mongoose": "^8.3.2"
  }
}
